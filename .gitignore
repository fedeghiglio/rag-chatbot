# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment / secrets — never commit credentials
.env
.env.*
*.env
# ...but the placeholder template SHOULD be committed.
!.env.example

# Local Claude Code settings (machine-specific allowlist, not shared config)
.claude/settings.local.json

# Project-local Claude Code instructions (not shared in the repo)
CLAUDE.md
